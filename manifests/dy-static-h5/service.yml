apiVersion: v1
kind: Service
metadata:
  name: dy-static-h5
  namespace: xinfa
  labels:
    app: dy-static-h5
    tier: frontend
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: dy-static-h5
    tier: frontend

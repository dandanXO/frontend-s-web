apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dy-static-h5
  namespace: xinfa
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-production"
spec:
  tls:
    - hosts:
        - dy-h5.jolly88-ph01.com
      secretName: dy-static-h5
    - hosts:
        - dy-h5.nxfxback.com
      secretName: dy-static-h5-nxfxback
  rules:
  - host: dy-h5.jolly88-ph01.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: dy-static-h5
            port:
              number: 80
  - host: dy-h5.nxfxback.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: dy-static-h5
            port:
              number: 80

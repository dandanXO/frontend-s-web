apiVersion: apps/v1
kind: Deployment
metadata:
  name: xf-static-web
  namespace: xinfa
  labels:
    app: xf-static-web
    tier: frontend
spec:
  selector:
    matchLabels:
      app: xf-static-web
      tier: frontend
  template:
    metadata:
      labels:
        app: xf-static-web
        tier: frontend
    spec:
      containers:
        - name: xf-static-web
          image: git.caspen-tech.com:5050/jolly88-complex/frontend:xf-static-web-1686215254 # {"$imagepolicy": "xinfa:xf-static-web"}
          ports:
            - containerPort: 80
          env:
            - name: TZ
              value: Asia/Singapore
          imagePullPolicy: Always
          resources:
            requests:
              memory: 256Mi
          readinessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: app
                    operator: In
                    values:
                      - xf-static-web
              topologyKey: kubernetes.io/hostname
      imagePullSecrets:
        - name: gitlab-registry

apiVersion: v1
kind: Service
metadata:
  name: ph-static-web
  namespace: xinfa
  labels:
    app: ph-static-web
    tier: frontend
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: ph-static-web
    tier: frontend

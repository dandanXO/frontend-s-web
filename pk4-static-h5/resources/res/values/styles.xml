<?xml version="1.0" encoding="utf-8"?>
<resources>

  <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>
  </style>

  <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="windowActionBar">false</item>
    <item name="windowNoTitle">true</item>
    <item name="android:background">@null</item>
  </style>

  <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
    <item name="android:windowBackground">@drawable/background_centered</item>
  </style>

  <style name="Theme.SplashTheme" parent="Theme.SplashScreen">
    <!-- 设置启动图标 -->
    <item name="windowSplashScreenAnimatedIcon">@drawable/background_centered</item>
    <!-- 设置启动背景色 -->
    <item name="windowSplashScreenBackground">@color/my_splash_background</item>
    <!-- 设置图标背景色 -->
    <item name="windowSplashScreenIconBackgroundColor">@color/colorPrimary</item>
    <!-- 设置启动后使用的主题 -->
    <item name="postSplashScreenTheme">@style/AppTheme</item>
  </style>
</resources>
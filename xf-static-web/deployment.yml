---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: xf-static-web
  namespace: xinfa
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-production"
spec:
  tls:
    - hosts:
        - xf-web.jolly88-ph01.com
      secretName: xf-static-web
  rules:
  - host: xf-web.jolly88-ph01.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: xf-static-web
            port:
              number: 80
---
apiVersion: v1
kind: Service
metadata:
  name: xf-static-web
  namespace: xinfa
  labels:
    app: xf-static-web
    tier: frontend
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: xf-static-web
    tier: frontend
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: xf-static-web
  namespace: xinfa
  labels:
    app: xf-static-web
    tier: frontend
spec:
  selector:
    matchLabels:
      app: xf-static-web
      tier: frontend
  template:
    metadata:
      labels:
        app: xf-static-web
        tier: frontend
    spec:
      containers:
        - name: xf-static-web
          image: git.caspen-tech.com:5050/jolly88-complex/frontend:xf-static-web
          ports:
            - containerPort: 80
          env:
            - name: TZ
              value: Asia/Singapore
          imagePullPolicy: Always
          resources:
            requests:
              memory: 256Mi
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: app
                    operator: In
                    values:
                      - xf-static-web
              topologyKey: kubernetes.io/hostname
      tolerations:
        - key: "performance"
          operator: "Equal"
          value: "frontend"
          effect: "PreferNoSchedule"
      imagePullSecrets:
        - name: gitlab-registry
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: xf-static-web
  namespace: xinfa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: xf-static-web
  minReplicas: 3
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: xf-static-web
  namespace: xinfa
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: xf-static-web

image: docker:20.10.16
services:
  - name: docker:20.10.16-dind
    alias: docker
    command: [ "--tls=false" ]
variables:
   DOCKER_DRIVER: overlay2
   DOCKER_TLS_CERTDIR: ""
   DOCKER_HOST: tcp://docker:2375
   NAMESPACE: xinfa

default:
  tags:
    - xinfa-runner

.build-projects:
  stage: build
  cache:
    paths:
      - node_modules
  image: node:16.17.0
  artifacts:
   untracked: true
   expire_in: 24 hrs
   exclude:
     - $CI_PROJECT_DIR/$PROJECT/node_modules/**/*
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'
      changes:
        - $PROJECT/**/*

.docker-build:
  stage: build-image
  script:
    - cd $PROJECT
    - docker build -t $CI_REGISTRY_IMAGE:$PROJECT -t $CI_REGISTRY_IMAGE:$PROJECT-$(date +%s) . --no-cache
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker push -a $CI_REGISTRY_IMAGE
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'
      changes:
        - $PROJECT/**/*

stages:
  - build
  - build-image

build-ph-static-h5:
  extends: .build-projects
  script:
    - cd ph-static-h5
    - npm install
    - npm install -g @quasar/cli
    - quasar build
  variables:
    PROJECT: ph-static-h5

build-image-ph-static-h5:
  extends: .docker-build
  variables:
    PROJECT: ph-static-h5
  needs:
    - build-ph-static-h5

#===============================================================================================

build-ph-static-web:
  extends: .build-projects
  script:
    - cd ph-static-web
    - npm install
    - npm run build
  variables:
    PROJECT: ph-static-web

build-image-ph-static-web:
  extends: .docker-build
  variables:
    PROJECT: ph-static-web
  needs:
    - build-ph-static-web

#===============================================================================================

build-tf-manager-web:
  extends: .build-projects
  script:
    - cd tf-manager-web
    - npm install
    - npm run build:prod
  variables:
    PROJECT: tf-manager-web

build-image-tf-manager-web:
  extends: .docker-build
  variables:
    PROJECT: tf-manager-web
  needs:
    - build-tf-manager-web

#===============================================================================================

build-xf-static-h5:
  extends: .build-projects
  script:
    - cd xf-static-h5
    - npm install
    - npm i process
    - npm install -g @quasar/cli
    - quasar build
  variables:
    PROJECT: xf-static-h5

build-image-xf-static-h5:
  extends: .docker-build
  variables:
    PROJECT: xf-static-h5
  needs:
    - build-xf-static-h5

#===============================================================================================

build-xf-static-web:
  extends: .build-projects
  script:
    - cd xf-static-web
    - npm install
    - npm run build
  variables:
    PROJECT: xf-static-web

build-image-xf-static-web:
  extends: .docker-build
  variables:
    PROJECT: xf-static-web
  needs:
    - build-xf-static-web
